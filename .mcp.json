{
  "mcpServers": {
    "unitares-governance": {
      "type": "http",
      "url": "${UNITARES_SERVER_URL:-http://localhost:8767}/mcp/",
      "headers": {
        "Authorization": "Bearer ${UNITARES_HTTP_API_TOKEN:-}"
      }
    }
  }
}
